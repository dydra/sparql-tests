(select (?name ?emp)
  (project (?name ?emp)
    (order ((asc ?emp))
      (bgp
        (triple ?x <http://xmlns.com/foaf/0.1/name> ?name)
        (triple ?x <http://example.org/things#empId> ?emp)
      ))))
